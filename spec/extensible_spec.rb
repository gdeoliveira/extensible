require 'spec_helper'

describe Extensible do
  it 'has a version number' do
    expect(Extensible::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
